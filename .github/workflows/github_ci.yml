name: Java CI

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 11
        uses: actions/setup-java@v2
        with:
          java-version: '11'
          distribution: 'adopt'
      - name: Compile with Maven
        run: mvn --update-snapshots compile
      - name: Run tests
        run: mvn test
      - name: send tg msg if failed
        if: ${{ failure() }}
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TG_TO }}
          token: ${{ secrets.TG_TOKEN }}
          message: |
            Дружочек-пирожочек! В нашем селе случилось несчастье - тесты не прошли.
            Село: ${{ github.repository }}
            Письмо: ${{ github.event.commits[0].message }}
            Адрес отправителя: https://github.com/${{ github.repository }}/commit/${{github.sha}}

            Хорошего тебе дня! 😹